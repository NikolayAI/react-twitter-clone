function* fetchUsersRequest() {
  // try {
  //   const items = yield call(usersApi.fetchUsers);
  //   yield put(setUsers(items));
  // } catch (error) {
  //   yield put(setUsersLoadingState(LoadingState.ERROR));
  // }
}

export function* usersSaga() {
  // yield takeLatest(UsersActionsType.FETCH_TAGS, fetchUsersRequest);
}